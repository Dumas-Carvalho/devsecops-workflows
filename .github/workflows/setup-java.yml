name: Set Up Java

on:
  workflow_call:

jobs:
  reusable_workflow_setup:
    runs-on: ubuntu-latest
    steps:
      - name: Configure Java
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven
